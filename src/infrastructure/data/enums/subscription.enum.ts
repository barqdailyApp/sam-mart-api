/**
 * @Enum string
 * @description User gender options
 * @key MALE: male
 * @key FEMALE: female
 */
export enum SubscriptionStatus {
  ACTIVE = 'Active',
  EXPIRED = 'expired',
  EMPTY = 'EMPTY',

  RESCHEDULED = 'rescheduled',
  Gift = 'gift',
}
